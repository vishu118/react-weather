import "./App.css";
import Counter from "./Counter";

function App() {
  const arr = [1, 2, 3, 4, 5];

  return (
    <>
      {/* {arr.map((ele) => (
        <div>{ele}</div>
      ))}

      <h1>hello</h1> */}
      <Counter></Counter>
    </>
  );
}

export default App;
